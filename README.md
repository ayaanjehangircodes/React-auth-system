# React-auth-system
React authentication system with login and registration, form validation, and protected routes. Built with React Router, Context API, and either a fake API or Firebase for authentication.
